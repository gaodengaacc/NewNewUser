<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="mvvm"
            type="com.lyun.user.viewmodel.RegisterVerifyPhoneViewModel"/>
    </data>

    <RelativeLayout
        android:id="@+id/activity_layout_root"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#ffffff"
        android:orientation="vertical"
        android:weightSum="4"
        tools:context="com.lyun.user.activity.RegisterVerifyPhoneActivity">

        <ImageView
            android:id="@+id/register_verify_phone_logo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_gravity="center"
            android:layout_marginTop="30dp"
            android:src="@mipmap/ic_logo_login"/>

        <RelativeLayout
            android:id="@+id/register_verify_phone_phone_container"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:layout_below="@id/register_verify_phone_logo"
            android:layout_marginLeft="40dp"
            android:layout_marginRight="40dp"
            android:layout_marginTop="50dp">

            <EditText
                android:layout_width="match_parent"
                android:layout_height="40dp"
                android:layout_gravity="center"
                android:background="@drawable/edittext_default_bg"
                android:cursorVisible="true"
                android:drawablePadding="5dp"
                android:gravity="center_vertical"
                android:hint="请输入您的手机号"
                android:inputType="number"
                android:maxLines="1"
                android:padding="10dp"
                android:singleLine="true"
                android:text="@={mvvm.username}"
                android:textCursorDrawable="@drawable/cursor_color"
                android:textSize="14sp"/>

            <ImageButton
                android:layout_width="40dp"
                android:layout_height="match_parent"
                android:layout_alignParentRight="true"
                android:background="@null"
                android:src="@mipmap/icon_common_text_clear"
                app:clickCommand="@{mvvm.onClearButtonClick}"/>
        </RelativeLayout>

        <RelativeLayout
            android:id="@+id/register_verify_phone_msg_code_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/register_verify_phone_phone_container"
            android:layout_marginLeft="40dp"
            android:layout_marginRight="40dp"
            android:layout_marginTop="10dp"
            android:orientation="horizontal"
            android:weightSum="10">

            <EditText
                android:layout_width="match_parent"
                android:layout_height="40dp"
                android:layout_gravity="center"
                android:background="@drawable/edittext_default_bg"
                android:cursorVisible="true"
                android:drawablePadding="5dp"
                android:gravity="center_vertical"
                android:hint="请输入验证码"
                android:inputType="number"
                android:maxLines="1"
                android:padding="10dp"
                android:text="@={mvvm.smscode}"
                android:textCursorDrawable="@drawable/cursor_color"
                android:textSize="14sp"/>

            <Button
                android:layout_width="wrap_content"
                android:layout_height="30dp"
                android:layout_alignParentRight="true"
                android:layout_centerVertical="true"
                android:layout_gravity="center"
                android:background="@{mvvm.clickable?@drawable/btn_get_msg_code_enable_bg:@drawable/btn_get_msg_code_disable_bg}"
                android:clickable="@{mvvm.clickable}"
                android:padding="0dp"
                android:text="@{mvvm.mSendSmsCode}"
                android:textColor="@android:color/white"
                android:textSize="14sp"
                app:clickCommand="@{mvvm.onGetSMSCodeButtonClick}"/>
        </RelativeLayout>

        <Button
            android:id="@+id/login_button"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_below="@id/register_verify_phone_msg_code_container"
            android:layout_marginLeft="40dp"
            android:layout_marginRight="40dp"
            android:layout_marginTop="15dp"
            android:background="@drawable/bg_no_border_round_corner"
            android:text="下一步"
            android:textColor="@android:color/white"
            android:textSize="16sp"
            app:clickCommand="@{mvvm.onNextButtonClick}"/>
    </RelativeLayout>
</layout>